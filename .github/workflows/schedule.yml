name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.e1dd76d60ec6a047a3883a6efdb4288d }}
          TIMEZONE: Asia/Seoul
