name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.https://gist.github.com/eogh234/ }}
          TIMEZONE: Asia/Seoul
